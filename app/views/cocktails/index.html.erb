<div class="container">
  <div class="banner">
    <h1>Mr Cocktails</h1>
  </div>

  <div id="align-cocktail-cards">
    <% @cocktails.each do |cocktail| %>
      <div class="cocktail-card">
        https://picsum.photos/200/300/?random

        <p><%= cocktail.id %>. <%= cocktail.name %> | <%= link_to "Show deails", cocktail_path(cocktail.id) %></p>
      </div>
    <% end %>
  </div>

  <%= link_to "Add a new cocktail", new_cocktail_path %>
  <br>
  <br>
</div>
